# DISEASE-PREDICTION-USING-PYTHON-AND-SCIKIT-LEARN
Disease prediction uses patient data (like glucose, BMI, age) to predict illness. Data is split into training/testing, scaled using StandardScaler, and trained with Logistic Regression. Model accuracy is evaluated, and new patient inputs are tested to predict disease (1) or no disease (0).
